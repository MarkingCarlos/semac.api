package com.semac.java_api.model.enums;

/* Como o `valor` de uma regra_xp é lido. PONTOS soma xp direto; MINUTOS
   é limite de tempo (os dois cortes de atraso do check-in). Separa o que
   entra na lista "como ganhar xp" do participante do que vira nota de
   rodapé. */
public enum UnidadeRegraXp {
    PONTOS,
    MINUTOS
}
